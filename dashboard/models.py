"""The dashboard app currently reuses domain models from users.models.

Activity history is stored in users.models.ActivityLog to preserve compatibility
with the existing mobile API and services.
"""
